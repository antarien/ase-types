# Stub CMakeLists.txt for ase-types
# TODO: Implement actual build logic

cmake_minimum_required(VERSION 3.20)
project(ase-types VERSION 0.1.0 LANGUAGES CXX)

message(STATUS "Stub: ase-types (not implemented yet)")

# Placeholder target
add_custom_target(ase-types_stub ALL
    COMMAND ${CMAKE_COMMAND} -E echo "Module ase-types is a stub"
)
